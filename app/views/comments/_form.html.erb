<%# @park parameter is the current instance of the park %>
<%# second parameter is possible through associations.  park has many comments. building a comment from the park  %>
<%= simple_form_for([@park, @park.comments.build]) do |f|%>
<%= f.input :comment %>
<%= f.button :submit %>
<% end %>